{
  "name": "image-replacer",
  "version": "1.1.0",
  "private": true,
  "description": "Chrome extension that replaces every image on a page with a colour mosaic, with right-click restore.",
  "scripts": {
    "test": "node test/e2e.js"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  },
  "license": "MIT"
}
